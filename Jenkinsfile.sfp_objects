node ('arma') {
  stage('Checkout from Git') {
    checkout scm
  }

  stage('Download latest SFP Objects') {
    bat 'gradlew sfp_objects:downloadMod'
  }

  stage('Run autotest') {
    bat 'gradlew sfp_objects:runAutotest'
  }

  stage('Copy Log File') {
    bat 'gradlew sfp_objects:copyLogFile'
  }

  stage('Archive Log File') {
  	archiveArtifacts artifacts: 'sfp_objects/logs/*.rpt'
  }
}
